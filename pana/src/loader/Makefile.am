bin_PROGRAMS = pana

INCLUDES = \
        $(all_includes)

pana_SOURCES = \
        loader.cpp

pana_LDADD = \
        $(LIB_QT) \
        $(LIB_KDEUI)

pana_LDFLAGS = $(all_libraries) $(KDE_RPATH)

METASOURCES = AUTO

